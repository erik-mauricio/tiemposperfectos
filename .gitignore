# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.env
**/.env

# Local-only notes
LOCAL_PR_SUMMARY.md

# Legacy code stashed during Phase 1 cleanup
server/legacy_spanish_app.py.bak

# Documentation (not yet ready to commit)
docs/

# Downloaded NLTK data (fetched on first run, not source)
ml/.nltk_data/

# Python bytecode caches
__pycache__/
*.pyc

# Trained artifacts / model registry (produced by ml/training/*, stays
# local per the "inference locally, training separately" principle;
# server/ only needs a registry entry + artifact to exist on disk)
ml/artifacts/
ml/registry/

# W&I+LOCNESS corpus (BEA-2019): non-commercial, no-redistribution license
# — must never be committed to this publicly cloneable repo. errors_v2.jsonl
# is derived from it and inherits the same restriction.
ml/datasets/wi+locness/
ml/datasets/errors_v2.jsonl
